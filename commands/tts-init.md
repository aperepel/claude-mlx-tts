---
allowed-tools: Bash(*)
description: Install MLX dependencies and download model (~4GB)
---

Run this command to install MLX TTS:

!`${CLAUDE_PLUGIN_ROOT}/scripts/tts-init.sh`

After completion, suggest running `/tts-start` to warm up the server.
