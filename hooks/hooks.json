{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "uv run --with mlx-audio --with librosa --with mlx-lm --with einops --with sounddevice --isolated python ${CLAUDE_PLUGIN_ROOT}/scripts/tts-notify.py",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
